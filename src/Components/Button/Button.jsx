import React from "react";

const Button = (props) => {
  return (
    <button style={{ backgroundColor: "red", pading: 100, width: 200 }}>
      button s
    </button>
  );
};

export default Button;
